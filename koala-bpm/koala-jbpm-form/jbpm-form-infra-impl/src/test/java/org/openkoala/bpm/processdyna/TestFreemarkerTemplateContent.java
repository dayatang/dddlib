package org.openkoala.bpm.processdyna;

import java.io.IOException;

import org.junit.Test;
import org.openkoala.koala.util.KoalaBaseSpringTestCase;

public class TestFreemarkerTemplateContent extends KoalaBaseSpringTestCase {

//	@Inject
//	private TemplateContent templateContent;

	@Test
	public void testProcess() throws IOException {

		/*DynaProcessKey date = new DynaProcessKey("aaa", "申请日期", DynaType.Date);

		Map<String, Object> params = new HashMap<String, Object>();
		params.put(date.getKeyId(), date);
		params.values();
		Map root = new HashMap();
		root.put("params", params);

		String data = "<div>${params[\"aaa\"].widget}</div>";
		// data+=marco;

		String value = templateContent.process(root, data);
		System.out.println(value);*/
	}
}
